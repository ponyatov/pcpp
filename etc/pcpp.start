#!/bin/sh
. $(dirname "$0")/pcpp.config

# sudo systemctl daemon-reload
# sudo systemctl enable pcpp@dev01
# sudo systemctl start pcpp@dev01
# sudo systemctl stop pcpp@dev01
# sudo systemctl status pcpp@dev01

echo 1 | tee /sys/module/vfio/parameters/enable_unsafe_noiommu_mode
# echo 1 | sudo tee /sys/module/vfio/parameters/enable_unsafe_noiommu_mode
# cat /sys/module/vfio/parameters/enable_unsafe_noiommu_mode

echo 2048 | tee /sys/kernel/mm/hugepages/hugepages-2048kB/nr_hugepages
# echo 8192 | sudo tee /sys/kernel/mm/hugepages/hugepages-2048kB/nr_hugepages
# cat /sys/kernel/mm/hugepages/hugepages-2048kB/nr_hugepages
# cat /proc/meminfo | grep Huge

# $DPDKPY setup -r $DPDK_ROOT -g $HUGESIZE -i $ETH -m $DRIVER

# sudo lib/pcpp/setup_dpdk.py status

# 0000:01:00.0
# 'MT27800 Family [ConnectX-5] 1017'
# drv=vfio-pci unused=mlx5_core,igb_uio,uio_pci_generic
